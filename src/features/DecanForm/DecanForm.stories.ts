import { DecanForm } from "@/features/DecanForm/DecanForm";
import type { Meta, StoryObj } from "@storybook/react";
const meta: Meta<typeof DecanForm> = { component: DecanForm };
export default meta;
type Story = StoryObj<typeof DecanForm>;

export const Base: Story = {};
